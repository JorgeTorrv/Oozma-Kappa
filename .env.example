# Copia este archivo a .env y ajusta los valores.
# Ruta del archivo SQLite (relativa a /prisma). En Docker se monta un volumen.
DATABASE_URL="file:./dev.db"

# Entorno: development | production
NODE_ENV="development"
